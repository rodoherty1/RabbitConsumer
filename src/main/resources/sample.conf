amqp {
  connections = [
    {
      ip           = "localhost"
      port         = 5672
      user         = "guest"
      password     = "guest"
      useSSL       = false
      exchangeName = "exchange.name"
      queue        = "queueName"
      routingKey   = ""
      fileName     = "~/scratch/ui-output.json"
    }
  ]
}